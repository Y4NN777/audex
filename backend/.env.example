# FastAPI settings
API_V1_PREFIX=/api/v1
PROJECT_NAME=AUDEX
BACKEND_CORS_ORIGINS=["http://localhost:5173"]
STORAGE_PATH=tmp/uploads

# Security (fill with real secrets in local .env)
JWT_SECRET_KEY=changeme
JWT_REFRESH_SECRET_KEY=changeme-refresh
